const config = {
  // 启动端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'o2o',
    USERNAME: 'root',
    PASSWORD: 'zjhl2',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config